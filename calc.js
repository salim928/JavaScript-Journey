
function mul(x, y) {
    result = x * y
    return result
}

function sum(x , y) {
    result = x + y
    return result;
}

function division(x , y) {
    result = x / y
    return result
}
function sub(x, y) {
    result = x - y
    return result
}

const prompt = require("prompt-sync")()
let x = parseFloat(prompt("Enter number 1: "))
let y = parseFloat(prompt("Enter number 1: "))

switch (key) {
    case value:
        
        break;

    default:
        break;
}

console.log("The result: ",sum(x,y))